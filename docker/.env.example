# Docker环境变量配置示例
# 复制此文件为.env并根据需要修改

# 时区设置
# 可选值: Asia/Shanghai, America/New_York, Europe/London等
TIMEZONE=Asia/Shanghai

# 使用的模型
# 可选值: gpt-4o, gpt-4-turbo, gpt-3.5-turbo等
MODEL=gpt-4o

# 是否在首次启动时立即执行任务（true或false）
IMMEDIATE_RUN=false

# 其他配置可以在config/config.py中设置 